export const metadata = {
    title: 'Events Details | TicketPass',
    description: 'Ticket',
  }
  
  export default function EventsDetailsLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    )
  }
  