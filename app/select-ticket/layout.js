export const metadata = {
    title: 'Select Ticket | TicketPass',
    description: 'Ticket',
  }
  
  export default function EventsLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    )
  }
  