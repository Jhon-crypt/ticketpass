export const metadata = {
  title: 'Events | TicketPass',
  description: 'Ticket',
}

export default function EventsLayout({ children }) {


  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  )
}
