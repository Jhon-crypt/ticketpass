export const metadata = {
  title: 'TicketPass',
  description: 'Sign in',
}

export default function SignInLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
