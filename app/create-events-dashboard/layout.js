export const metadata = {
    title: 'Create events | TicketPass',
    description: 'Events',
  }
  
  export default function EventsLayout({ children }) {
  
  
    return (
      <html lang="en">
        <body>
          {children}
        </body>
      </html>
    )
  }
  