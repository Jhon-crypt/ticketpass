export const metadata = {
    title: 'Tickets | TicketPass',
    description: 'Ticket',
  }
  
  export default function TicketsLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    )
  }
  