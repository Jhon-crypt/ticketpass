export const metadata = {
    title: 'Picked Tickets | TicketPass',
    description: 'Ticket',
  }
  
  export default function TicketsPickedLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    )
  }
  