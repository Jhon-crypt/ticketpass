export const metadata = {
  title: 'Account Type | Ticket Pass',
  description: 'Digitizing Tickets On The Blockchain',
}

export default function AccountLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
