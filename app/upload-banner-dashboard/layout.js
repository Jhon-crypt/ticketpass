export const metadata = {
    title: 'Upload Banner | TicketPass',
    description: 'Events',
  }
  
  export default function EventsLayout({ children }) {
  
  
    return (
      <html lang="en">
        <body>
          {children}
        </body>
      </html>
    )
  }
  